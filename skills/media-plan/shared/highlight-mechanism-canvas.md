# Highlight Mechanism Canvas

Use this file as the canonical contract for turning a viable idea direction into proposal-ready creative assets. A direction is not ready for execution or proposal unless its highlight mechanism is explicit.

## Required Fields

- `Theme`: one-sentence core theme that can anchor the proposal title or chapter headline.
- `Core tension`: combine product truth, audience tension, and relevant scene or regional context.
- `Participation mechanism`: why users, creators, or partners will join.
- `Spread mechanism`: why the idea is likely to earn discussion, UGC, or platform-native spread.
- `Conversion mechanism`: how attention moves toward lead capture, store visit, trial, booking, or other stated success actions.
- `Hero event`: the single highest-signal activation, event, or flagship content moment.
- `Stage pillars` (mandatory): named, schedulable activations per macro stage — not a repeat of generic “media flight” lines. For **Early**, **Mid**, and **Late**, each stage must list **at least three** named pillar lines **or** cover all three roles **声量** (reach / buzz) **/ 圈层集客** (community / lead) **/ 转化承接** (conversion / handoff) with at least one named activity per role. Each line: short name + role tag(s). This is the “event menu” grain; it must not collapse to “Hero + channel table only.”
- `Local life map` (mandatory): for `geography_or_region`, list **at least three** concrete city-life slices (e.g. river/lake, core mall district, night economy, alley / “烟火”, cultural landmark) that are **not** only industrial-park or “车谷-only” narrative unless the brief is explicitly B2B / cluster-only.
- `Calendar alignment` (mandatory): at least **two** entries pairing **calendar month or week window** with a **citywide or industry event** to ride (sports, tourism festival, major fair, etc.). If research cannot substantiate, mark `TBC` and add a line to proposal `Open Questions`.
- `Stage choreography`: what pre-heat, launch, and sustain phases each need to accomplish **as narrative roles** (how stages differ in job-to-be-done). Do not duplicate `Stage pillars` verbatim; choreography = **why** each stage exists; pillars = **what** is named on the calendar.
- `Channel choreography`: how online, offline, creator, media, store, or private-domain actions relay rather than repeat.
- `Defensibility`: why the mechanism is brand-specific instead of template reuse.
- `Execution red lines`: timeline, budget, approval, supply, legal, or brand-safety boundaries that cannot be crossed.

## Output Shape

```md
## Highlight Mechanism Canvas

### [Direction name]
- Theme:
- Core tension:
- Participation mechanism:
- Spread mechanism:
- Conversion mechanism:
- Hero event:
- Stage pillars:
  - Early:
    - [name] — [声量|圈层|转化] …
    - (≥3 lines or 声量+圈层+转化 each named)
  - Mid:
    - …
  - Late:
    - …
- Local life map:
  - …
  - …
  - …
- Calendar alignment:
  - …
  - …
- Stage choreography:
  - Early:
  - Mid:
  - Late:
- Channel choreography:
- Defensibility:
- Execution red lines:
```

## Rules

- Keep the theme concrete enough to be reused in proposal chapter titles and execution naming.
- `Core tension` must not be generic category language; it should tie to the actual brief and research pack.
- If participation, spread, or conversion logic is weak or missing, the idea is not ready for downstream planning.
- `Hero event` can be online, offline, or hybrid, but it must clearly carry the theme instead of acting as a disconnected stunt.
- `Stage choreography` should describe role separation, not a repeated list of similar actions.
- `Stage pillars` must include **at least two distinct formats** of on-the-ground or content action in **each** of Early / Mid / Late (e.g. online UGC program + offline pop-up; or private salon + landmark drop) where the brief allows geography and channels; do not satisfy with a single hero plus undifferentiated “placement.”
- `Defensibility` must explain why a competitor cannot copy the idea with only minor wording swaps.
- `Execution red lines` should be concise and real; do not use placeholder risk language.
