# Proposal Template

## Entry Conditions

- Intake status is `ready`.
- Research verdict is `pass`.
- `## Selected Direction Handoff` is present for the chosen direction set (primary + any co-directions).
- `## Highlight Mechanism Canvas` is present for the selected direction and is complete enough to carry into execution, including `Stage pillars`, `Local life map`, and `Calendar alignment`.
- `## Validated Execution-Plan Handoff` is present for the recommended execution plan and has `Validation status: validated`.
- At least one idea is scored as `keep` or otherwise confirmed viable.
- `deliverables_needed`, `constraints`, and `success_metric` are all present in the brief and are not `unknown`, `tbd`, or similar placeholders.
- If any entry condition fails, stop proposal writing and return the blocking gap instead.

## Structure

The final proposal must show a visible decision trail in this order: brief -> research -> ideas -> recommendation.

1. `Brief Snapshot`
2. `Objective And Success Metric`
3. `Audience And Context`
4. `Research-Based Insights`
5. `Strategic Direction`
6. `Highlight Mechanism Overview`
7. `Core Idea Options`
8. `Phased Execution Plan`
9. `Phased Event Menu` (activity-level roster aligned with the execution handoff)
10. `Core Events, Channels, And Conversion Path`
11. `Risks And Open Questions`
12. `Decision Request And Next Step`

## Template

```md
# Proposal Title

## 1. Brief Snapshot

## 2. Objective And Success Metric

## 3. Audience And Context

## 4. Research-Based Insights

## 5. Strategic Direction

## 6. Highlight Mechanism Overview

## 7. Core Idea Options

## 8. Phased Execution Plan

## 9. Phased Event Menu

| Stage | Time (month / week / T±) | Activity name | Pillar type (声量 / 圈层 / 转化) | Format and venue or touchpoint | Link to main idea / Hero |
| --- | --- | --- | --- | --- | --- |

(Include at least three named rows per macro campaign stage — Early, Mid, Late — matching `Stage pillars` and `Pillar roster by phase` naming.)

## 10. Core Events, Channels, And Conversion Path

## 11. Risks And Open Questions

## 12. Decision Request And Next Step
```

## Writing Rules

- Keep the recommendation traceable to the brief and research.
- Make the brief -> research -> ideas -> recommendation trail explicit so reviewers can see why the recommendation follows from the earlier sections.
- Write in direct business language, not workshop jargon.
- Use `Highlight Mechanism Overview` to show the core theme, hero event, and participation / spread / conversion logic explicitly.
- Prefer concrete tradeoffs over broad possibility lists.
- Show what should happen next, who needs to decide, and what is still uncertain.
- Do not include execution recommendations that the stated constraints cannot support.
- **`Phased Event Menu` must use schedulable, name-level activity lines**, not a copy-paste of strategy headlines alone; keep KPIs, scoring tables, and long proof blocks in other sections. Row labels should match `Pillar roster by phase` in `## Validated Execution-Plan Handoff` unless a gap is explained.
